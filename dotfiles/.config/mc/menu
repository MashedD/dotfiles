t   Edit todo.txt
    $EDITOR ~/Documents/todo.txt

n   Edit notes.md
    $EDITOR ~/Documents/notes.md

b   Edit buy.txt
    $EDITOR ~/Documents/buy.txt

c   Edit calendar.txt
    $EDITOR ~/Documents/calendar.txt

s   Edit snippets.md
    $EDITOR ~/Documents/snippets.md

1   Edit trading-personal-notes.md
    $EDITOR ~/Documents/notes/trading-personal-notes.md

2   Edit trading.md
    $EDITOR ~/Documents/notes/trading.md

3   Edit notes
    $EDITOR ~/Documents/notes/

a   Edit artix.md
    $EDITOR ~/Documents/notes/artix.md

q   Play Quake
    ~/Projects/scripts/play-ironwail.sh

w   Play Quake 2 Bots
    ~/Projects/scripts/play-q2pro-bots.sh

j   Play Quake 2 Jump Mod
    ~/Projects/scripts/play-q2pro-jump.sh

z   Compress folder to .tar.gz
    CURRENT_FOLDER_NAME=$(basename %d /)
    echo -n "Name of the compressed file (without extension) [$CURRENT_FOLDER_NAME]: "
    read ARCHIVE_NAME
    [ "$ARCHIVE_NAME"x = x ] && ARCHIVE_NAME="$CURRENT_FOLDER_NAME"
    cd ..
    tar cf - "$CURRENT_FOLDER_NAME" | gzip -f9 > "$ARCHIVE_NAME.tar.gz"
    echo "../$ARCHIVE_NAME.tar.gz created"

x   Compress folder to .zip with password
    CURRENT_FOLDER_NAME=$(basename %d /)
    echo -n "Name of the compressed file (without extension) [$CURRENT_FOLDER_NAME]: "
    read ARCHIVE_NAME
    [ "$ARCHIVE_NAME"x = x ] && ARCHIVE_NAME="$CURRENT_FOLDER_NAME"
    zip -re "../$ARCHIVE_NAME.zip" .

